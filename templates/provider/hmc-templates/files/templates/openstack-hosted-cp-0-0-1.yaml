apiVersion: hmc.mirantis.com/v1alpha1
kind: ClusterTemplate
metadata:
  name: openstack-hosted-cp-0-0-1
  annotations:
    helm.sh/resource-policy: keep
spec:
  helm:
    chartSpec:
      chart: openstack-hosted-cp
      version: 0.0.1
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: hmc-bnr
